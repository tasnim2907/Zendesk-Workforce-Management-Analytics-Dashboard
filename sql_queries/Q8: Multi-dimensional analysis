-- Complete agent-status-priority matrix
SELECT 
  agent_name,
  status,
  priority,
  COUNT(*) as ticket_count,
  ROUND(AVG(resolution_time_hours), 2) as avg_resolution
FROM `zendesk-wfm-analytics.zendesk_support.tickets`
GROUP BY agent_name, status, priority
ORDER BY agent_name, status, priority
